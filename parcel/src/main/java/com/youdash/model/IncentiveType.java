package com.youdash.model;

public enum IncentiveType {
    ONLINE_HOURS_DAILY,
    DAILY_DELIVERIES_SLOT,
    // Legacy DB value retained for backward compatibility.
    BONUS,
    // Legacy DB value retained for backward compatibility.
    DAILY
}
